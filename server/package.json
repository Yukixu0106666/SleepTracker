{
  "name": "sleeptracker-server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=../.env.ingestion ingestion.mjs",
    "migrate": "node --env-file-if-exists=../.env.ingestion migrate.mjs",
    "credential": "node --env-file-if-exists=../.env.ingestion credential.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
